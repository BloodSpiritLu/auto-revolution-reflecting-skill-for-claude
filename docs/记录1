你大概看下这个介绍吧，这个主要是claude的用法，参考官方文档说说看怎么做：这段视频介绍的是在 Claude Code（Anthropic 推出的命令行 AI 编程助手）中实现“自我进化技能（Self-Improving Skills）”的方法。其核心目标是解决 LLM “没有长期记忆”的问题，让你对 AI 的每一次纠正都能沉淀为它永久掌握的知识。
要弄一个属于你自己的“自我进化”系统，可以参考以下原理和步骤：
核心原理：反馈闭环（Feedback Loop）
传统的 AI 助手每次开启新对话都是“白纸一张”。
1. 信号捕获：你在对话中对 AI 的纠正（例如：“以后处理这个模块要加 SQL 注入检查”）就是信号。
2. 反射（Reflect）：通过一个特定的“反射技能”，让 AI 扫描当前的对话历史。
3. 持久化：AI 将提取出的规则写入到一个本地的 Markdown 技能文件中。
4. 版本控制：配合 Git，你可以清晰地看到 AI 的“进化历程”，并在出现偏差时回滚。
具体实施步骤
1. 准备环境
你需要安装并配置好 Claude Code。Claude Code 允许用户在特定的目录下放置 .md 文件来定义“技能（Skills）”，这些文件会作为 AI 的系统提示词（System Prompt）的一部分。
2. 创建“反射技能”（Reflect Skill）
你需要编写一个“元技能”（Meta-Skill），它的任务是“教导 AI 如何更新自己的技能”。
* 在你的技能文件夹中创建一个 reflect.md。
* 在该文件中定义：当收到 /reflect 指令时，AI 应该分析本次会话，寻找：
   * 高置信度信号：用户明确要求的规范或禁止项。
   * 中置信度信号：在本次会话中运行成功的模式。
   * 低置信度信号：值得后续观察的习惯。
3. 建立技能存储库
* 为不同的任务创建不同的技能文件，例如 frontend_standards.md 或 api_design.md。
* 将这些文件放入 Git 管理。这样当 AI 通过 reflect 技能修改这些文件时，你可以通过 git diff 查看它学到了什么。
4. 实现自动化（高级用法：Hooks）
视频提到了通过 Hooks（钩子） 实现自动化：
* 手动模式：在对话结束前，输入 /reflect，手动确认 AI 提炼的改进点。
* 自动模式：利用 Claude Code 的 stop 钩子。编写一个简单的 Shell 脚本，在退出 Claude Code 时自动触发反射命令。这样 AI 就会在后台默默地完成“复盘”并更新技能文件。
5. 持续迭代
当你发现 AI 在某个地方又犯了错，你只需要在对话中纠正它，然后再次运行反射。随着时间的推移，你的技能文件夹会变成一个高度个性化的、最懂你代码风格的“私人专家库”。
总结建议
如果你想开始动手：
1. 先从手动模式开始，创建一个简单的技能文件，试着让 Claude 根据你的纠正去修改它 [02:14]。
2. 引入 Git。这是观察 AI 是否“学歪了”最重要的手段 [04:43]。
3. 关注视频中提到的 Confidence Levels（置信度等级） 概念，这能防止 AI 把偶尔的尝试当成必须遵守的死理 [03:33]。
这个系统的美妙之处在于它不需要复杂的向量数据库（Embeddings），仅仅通过读写 Markdown 文件就能实现非常有感的“经验积累”。